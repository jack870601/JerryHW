def _calculate(x,y,operator):
    if(operator == '+'):
        return (x + y)
    if(operator == '-'):
        return (x - y)
    if(operator == '*'):
        return (x * y)
    if(operator == '/'):
        return (x / y)
